var fetch = require('../lib/fetch'),
    $ = {
      ajax: function () {}
    };

describe("fetch", function () {
  describe("section", function () {
    it("fetches the required stories", function () {
    });

    describe("when the request is complete", function () {
      it("calls the callback", function () {
      });
    });
  });
});
